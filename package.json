{
  "name": "sentimind",
  "version": "1.1.0",
  "private": true,
  "description": "Sentimind - Brand sentiment analysis platform",
  "scripts": {
    "dev": "concurrently -n backend,frontend,collector,sentiment -c blue,cyan,green,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix collector-service\" \"npm run dev --prefix sentiment-service\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:collector": "npm run dev --prefix collector-service",
    "dev:sentiment": "npm run dev --prefix sentiment-service",
    "dev:celery": "npm run celery --prefix sentiment-service"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
